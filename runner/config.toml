# Global max turn limit per run. A model entry may override it with its own
# `max_turns = <n>` (see the commented example below) to give weaker models
# more attempts before the run is killed and flagged as maxTurnsExceeded.
max_turns = 100
run_rules = "run-rules.md"

# [[models]]
# name = "Qwen3.6-27B-Fable-Fusion-711-MTP-Q5_K_M"
# provider = "llamacpp-local"
# modelId = "qwen3.6-27b-fable-fusion-q5"
# max_turns = 200  # optional per-model override of the global limit
# notes = "llama.cpp server-side sampling defaults recorded for archival purposes"
# [models.params]
# thinking = "on"
# temp = 0.6
# top_k = 20
# top_p = 0.95
# repeat_penalty = 1.0
# spec_type = "draft-mtp"
# spec_draft_n_max = 2

# [[models]]
# name = "Qwen3.6-27B-Q5_K_M"
# provider = "llamacpp-local"
# modelId = "qwen3.6-27b-q5"
# notes = "llama.cpp server-side sampling defaults recorded for archival purposes"
# [models.params]
# thinking = "on"
# temp = 0.6
# top_k = 20
# top_p = 0.95
# repeat_penalty = 1.0
# spec_type = "draft-mtp"
# spec_draft_n_max = 2

[[models]]
name = "Qwopus3.6-35B-A3B-v1-MTP-Q5_K_M"
provider = "llamacpp-local"
modelId = "qwopus-35b-a3b-q5"
max_turns = 200
notes = ""
[models.params]
thinking = "off"
temp = 1.0
top_k = 20
top_p = 0.95
repeat_penalty = 1.0
spec_type = "draft-mtp"
spec_draft_n_max = 2

[[models]]
name = "Qwen3.6-35B-A3B-UD-Q5_K_M"
provider = "llamacpp-local"
modelId = "qwen3.6-35b-a3b-q5"
max_turns = 200
notes = ""
[models.params]
thinking = "off"
temp = 1.0
top_k = 20
top_p = 0.95
repeat_penalty = 1.0
spec_type = "draft-mtp"
spec_draft_n_max = 2
